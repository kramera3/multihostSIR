% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_fixed_trajectory}
\alias{plot_fixed_trajectory}
\title{Epidemic trajectory for a fixed community}
\usage{
plot_fixed_trajectory(x, by_species = FALSE)
}
\arguments{
\item{x}{A \code{"multihost_fixed"} object from \code{\link[=run_fixed_community]{run_fixed_community()}}.}

\item{by_species}{If \code{TRUE}, draw one curve per species; if \code{FALSE} (default),
draw the community total.}
}
\value{
A ggplot object.
}
\description{
Plots the community-wide number infected over time for a deterministic run
from \code{\link[=run_fixed_community]{run_fixed_community()}}. With \code{by_species = TRUE}, one line is drawn per
species instead of the community total.
}
\examples{
species <- data.frame(
  type = c("Prey", "Prey", "Predator"),
  mass = c(0.02, 50, 5), mu = c(0.013, 0.002, 0.003),
  gamma = c(0.27, 0.04, 0.07), alpha = c(0.027, 0.004, 0.007),
  N = c(9000, 27, 6), competence = c(0.5, 0.1, 0.2))
comm <- read_community(species)
beta <- read_transmission_matrix(matrix(c(
  0.05, 0.01, 0, 0.01, 0.03, 0, 0.02, 0.02, 0.04), nrow = 3, byrow = TRUE),
  community = comm)
fit <- run_fixed_community(comm, beta, duration = 150)
plot_fixed_trajectory(fit)
plot_fixed_trajectory(fit, by_species = TRUE)
}
\seealso{
\code{\link[=run_fixed_community]{run_fixed_community()}}, \code{\link[=plot.multihost_fixed]{plot.multihost_fixed()}}
}
