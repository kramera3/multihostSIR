% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{run_multihost_sir}
\alias{run_multihost_sir}
\alias{print.multihost_sir}
\title{Replicate multi-host epidemics across community sizes}
\usage{
run_multihost_sir(
  richness_levels = 3:8,
  n_iterations = 100,
  duration = 365,
  spec = community_spec(),
  allo = allometry(),
  coefficients = trophic_coefficients(),
  seed = NULL,
  parallel = FALSE,
  n_cores = 2L,
  warmup = TRUE,
  progress = FALSE,
  ...
)

\method{print}{multihost_sir}(x, ...)
}
\arguments{
\item{richness_levels}{Integer vector of community sizes to simulate. Each
value must be at least 1; at least 2 species are needed for any
between-species transmission to occur.}

\item{n_iterations}{Number of stochastic replicate communities per richness
level.}

\item{duration}{Simulation horizon in days.}

\item{spec}{A specification from \code{\link[=community_spec]{community_spec()}}.}

\item{allo}{An allometry object from \code{\link[=allometry]{allometry()}}.}

\item{coefficients}{Trophic coefficients from \code{\link[=trophic_coefficients]{trophic_coefficients()}}.}

\item{seed}{Optional integer seed applied before the timed loop, for
reproducibility. \code{NULL} (default) leaves the RNG untouched.}

\item{parallel}{If \code{TRUE}, distribute iterations across a PSOCK cluster.
Defaults to \code{FALSE}, which keeps the default run single-threaded.}

\item{n_cores}{Number of worker processes when \code{parallel = TRUE}. Defaults to
\code{2}, the maximum permitted in CRAN checks. Raise it for real work.}

\item{warmup}{If \code{TRUE} (default), a short untimed batch is run first so that
one-off costs such as cluster start-up and JIT compilation do not inflate
the timing of the first richness level.}

\item{progress}{If \code{TRUE}, report progress after each richness level.}

\item{...}{Ignored.}

\item{x}{A \code{"multihost_sir"} object.}
}
\value{
An object of class \code{"multihost_sir"}: a list with elements
\describe{
\item{results}{Data frame with one row per iteration.}
\item{trajectories}{Data frame of mean daily prevalence per richness level.}
\item{timing}{Data frame of elapsed time per richness level.}
\item{settings}{The inputs used.}
\item{call}{The matched call.}
}
}
\description{
Runs \code{n_iterations} independent random communities at each requested richness
level, recording epidemiological outcomes, the mean epidemic trajectory, and
the wall-clock time taken by each level. The timing table supports the
run-time scaling study described in \code{\link[=timing_complexity]{timing_complexity()}}.
}
\examples{
# Small, fast example. Use n_iterations in the hundreds or thousands for
# publishable results.
res <- run_multihost_sir(richness_levels = 2:4, n_iterations = 5,
                         duration = 120, seed = 1)
res
summary(res)

\donttest{
# A larger run, in parallel
big <- run_multihost_sir(richness_levels = 3:8, n_iterations = 200,
                         seed = 42, parallel = TRUE, n_cores = 2)
plot(big, which = "r0")
}

}
\seealso{
\code{\link[=simulate_once]{simulate_once()}}, \code{\link[=timing_complexity]{timing_complexity()}}, \code{\link[=export_results]{export_results()}}
}
