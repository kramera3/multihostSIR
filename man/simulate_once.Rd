% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_once}
\alias{simulate_once}
\title{Simulate one random community from end to end}
\usage{
simulate_once(
  richness,
  spec = community_spec(),
  allo = allometry(),
  coefficients = trophic_coefficients(),
  duration = 365,
  ...
)
}
\arguments{
\item{richness}{Number of host species.}

\item{spec}{A specification from \code{\link[=community_spec]{community_spec()}}.}

\item{allo}{An allometry object from \code{\link[=allometry]{allometry()}}.}

\item{coefficients}{Trophic coefficients from \code{\link[=trophic_coefficients]{trophic_coefficients()}}.}

\item{duration}{Simulation horizon in days.}

\item{...}{Passed to \code{\link[=simulate_epidemic]{simulate_epidemic()}}.}
}
\value{
A list with elements \code{row} (one-row data frame), \code{prevalence}
(numeric vector of daily total infected) and \code{time}.
}
\description{
Draws a community, builds its transmission matrix, computes \eqn{R_0}, solves
the SIR system, and returns a one-row summary together with the daily
community-wide prevalence series. This is the unit of work replicated by
\code{\link[=run_multihost_sir]{run_multihost_sir()}}.
}
\examples{
set.seed(42)
one <- simulate_once(5, duration = 200)
one$row[, c("richness", "R0", "peak_prevalence", "days_to_peak")]

}
\seealso{
\code{\link[=run_multihost_sir]{run_multihost_sir()}}
}
