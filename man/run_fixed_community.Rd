% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{run_fixed_community}
\alias{run_fixed_community}
\alias{print.multihost_fixed}
\title{Run one fixed, fully specified community (no iteration)}
\usage{
run_fixed_community(
  community,
  beta = NULL,
  duration = 365,
  coefficients = trophic_coefficients(),
  ...
)

\method{print}{multihost_fixed}(x, ...)
}
\arguments{
\item{community}{A community from \code{\link[=read_community]{read_community()}} or \code{\link[=build_community]{build_community()}}.}

\item{beta}{A transmission matrix from \code{\link[=read_transmission_matrix]{read_transmission_matrix()}}, or any
\eqn{n \times n} numeric matrix. If \code{NULL}, the matrix is built from the
community with \code{\link[=transmission_matrix]{transmission_matrix()}} and \code{coefficients} (in which case the
run is still deterministic, but uses the package's transmission rules rather
than your own numbers).}

\item{duration}{Simulation horizon in days.}

\item{coefficients}{Trophic coefficients, used only when \code{beta = NULL}.}

\item{...}{Ignored.}

\item{x}{A \code{"multihost_fixed"} object.}
}
\value{
An object of class \code{"multihost_fixed"}: a list with \code{results} (a
one-row data frame of community-level outcomes), \code{species} (per-species
metrics), \code{trajectory} (daily prevalence), \code{R0}, \code{community}, \code{beta}, and
\code{assumptions} (a record of what was supplied versus filled in).
}
\description{
When a community and its transmission matrix are both supplied, the epidemic
is deterministic: there is nothing random to average over, so the model is
solved exactly once. This is the counterpart to \code{\link[=run_multihost_sir]{run_multihost_sir()}}, which
repeats random draws.
}
\examples{
species <- data.frame(
  type       = c("Prey", "Prey", "Predator"),
  mass       = c(0.02, 50, 5),
  mu         = c(0.013, 0.002, 0.003),
  gamma      = c(0.27, 0.04, 0.07),
  alpha      = c(0.027, 0.004, 0.007),
  N          = c(9000, 27, 6),
  competence = c(0.5, 0.1, 0.2))
comm <- read_community(species)
beta <- read_transmission_matrix(matrix(c(
  0.05, 0.01, 0.00,
  0.01, 0.03, 0.00,
  0.02, 0.02, 0.04), nrow = 3, byrow = TRUE), community = comm)

fit <- run_fixed_community(comm, beta, duration = 200)
fit
fit$results[, c("richness", "R0", "peak_prevalence")]

}
\seealso{
\code{\link[=run_multihost_sir]{run_multihost_sir()}}, \code{\link[=read_community]{read_community()}},
\code{\link[=read_transmission_matrix]{read_transmission_matrix()}}
}
