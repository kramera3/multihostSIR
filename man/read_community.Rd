% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_community}
\alias{read_community}
\title{Read a host community from a data frame, CSV, or Excel file}
\usage{
read_community(x, allo = allometry(), sheet = NULL)
}
\arguments{
\item{x}{A data frame, or a path to a \code{.csv}, \code{.xlsx}, or \code{.xls} file.}

\item{allo}{An allometry object from \code{\link[=allometry]{allometry()}}, used to fill any missing
rate columns.}

\item{sheet}{For Excel input, the sheet name or number. Defaults to the first
sheet.}
}
\value{
A community data frame with the columns \code{\link[=build_community]{build_community()}} produces,
carrying an attribute \code{"complete"} that records whether every rate was
supplied by the user (\code{TRUE}) or any were filled from allometry (\code{FALSE}).
}
\description{
Turns a species table into the community object the rest of the package uses.
The input may be a data frame already in memory, a path to a \code{.csv} file, or a
path to an Excel \code{.xlsx}/\code{.xls} file.
}
\details{
One row per species. The columns are:

\tabular{lll}{
\strong{Column} \tab \strong{Meaning} \tab \strong{Needed?} \cr
\code{type}       \tab \code{"Prey"} or \code{"Predator"}          \tab always \cr
\code{mass}       \tab body mass (kg)                    \tab always \cr
\code{mu}         \tab background mortality (per day)    \tab for a complete table \cr
\code{gamma}      \tab recovery rate (per day)           \tab for a complete table \cr
\code{alpha}      \tab disease-induced mortality (per day) \tab for a complete table \cr
\code{N}          \tab density (individuals per km^2)    \tab for a complete table \cr
\code{competence} \tab within-species transmission \eqn{\beta} \tab for a complete table \cr
\code{species}    \tab a name or label                  \tab optional \cr
}

If a rate column is absent it is filled from body mass using \code{allo}, exactly
as \code{\link[=build_community]{build_community()}} would. A table is "complete" only when every column
above is present and supplied for every species. Reading Excel needs the
\pkg{readxl} package.
}
\examples{
# From a data frame already in memory
species <- data.frame(
  species    = c("mouse", "deer", "fox"),
  type       = c("Prey", "Prey", "Predator"),
  mass       = c(0.02, 50, 5),
  mu         = c(0.013, 0.002, 0.003),
  gamma      = c(0.27, 0.04, 0.07),
  alpha      = c(0.027, 0.004, 0.007),
  N          = c(9000, 27, 6),
  competence = c(0.5, 0.1, 0.2))
comm <- read_community(species)
attr(comm, "complete")   # TRUE: nothing was filled in

\dontrun{
# From files
comm <- read_community("my_species.csv")
comm <- read_community("my_species.xlsx", sheet = "communities")
}

}
\seealso{
\code{\link[=read_transmission_matrix]{read_transmission_matrix()}}, \code{\link[=run_fixed_community]{run_fixed_community()}},
\code{\link[=build_community]{build_community()}}
}
