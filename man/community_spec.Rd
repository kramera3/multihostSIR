% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community.R
\name{community_spec}
\alias{community_spec}
\alias{print.multihost_community_spec}
\title{Rules for assembling random host communities}
\usage{
community_spec(
  prey_prob = 0.8,
  prey_masses = c(0.02, 0.1, 2, 10, 50),
  predator_masses = c(1, 5, 25),
  predator_density_factor = 0.1,
  competence_ranges = list(Low = c(0.01, 0.1), Med = c(0.1, 0.3), High = c(0.3, 0.8)),
  competence_log_scale = TRUE
)

\method{print}{multihost_community_spec}(x, ...)
}
\arguments{
\item{prey_prob}{Probability that any one species is prey. Predators make up
the remainder. Must lie in \verb{[0, 1]}.}

\item{prey_masses, predator_masses}{Numeric vectors of candidate body masses
(kg) for prey and predators. Masses are sampled from these pools with
replacement.}

\item{predator_density_factor}{Predator densities are multiplied by this
factor after allometric scaling, representing top-down thinning of upper
trophic levels. \code{0.1} means predators occur at one tenth of their
allometric baseline density.}

\item{competence_ranges}{A named list of length-2 numeric vectors giving the
range of the within-species competence parameter \eqn{\beta} for each
competence category. Names become the category labels.}

\item{competence_log_scale}{If \code{TRUE} (default), competence is drawn
uniformly on the log scale and back-transformed, which spreads draws evenly
across orders of magnitude. If \code{FALSE}, competence is drawn uniformly on
the linear scale.}

\item{x}{An object of class \code{"multihost_community_spec"}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"multihost_community_spec"}.
}
\description{
Bundles every choice that governs how a random community is drawn: the
probability that a species is prey, the body-mass pools available to each
trophic group, the thinning applied to predator densities, and the ranges
from which host competence is sampled.
}
\examples{
community_spec()

# Larger-bodied prey pool, predators at 25\% of baseline density
community_spec(prey_masses = c(5, 20, 80), predator_density_factor = 0.25)

}
\seealso{
\code{\link[=build_community]{build_community()}}, \code{\link[=allometry]{allometry()}}
}
