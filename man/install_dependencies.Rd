% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{install_dependencies}
\alias{install_dependencies}
\title{Install missing dependencies}
\usage{
install_dependencies(
  include_optional = TRUE,
  ask = interactive(),
  lib = .libPaths()[1L],
  repos = getOption("repos"),
  ...
)
}
\arguments{
\item{include_optional}{Also install the optional packages.}

\item{ask}{Prompt before installing. Defaults to \code{\link[=interactive]{interactive()}}.}

\item{lib}{Library directory to install into. Defaults to the first element
of \code{\link[=.libPaths]{.libPaths()}}.}

\item{repos}{CRAN mirror to install from.}

\item{...}{Passed to \code{\link[utils:install.packages]{utils::install.packages()}}.}
}
\value{
Invisibly, a character vector of the packages that were installed.
}
\description{
Installs any package that \code{\link[=check_dependencies]{check_dependencies()}} reports as missing or older
than the minimum version required. Packages that are already current are left
untouched.
}
\details{
Nothing is installed without a decision. In an interactive session you are
asked to confirm; in a non-interactive session you must pass \code{ask = FALSE}
explicitly, which makes the function safe to call from scripts that are meant
to install things and inert everywhere else.
}
\examples{
\dontrun{
# Interactive: prompts before doing anything
install_dependencies()

# Scripted: required packages only, no prompt
install_dependencies(include_optional = FALSE, ask = FALSE)
}

}
\seealso{
\code{\link[=check_dependencies]{check_dependencies()}}
}
