% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_results}
\alias{export_results}
\title{Export results to disk}
\usage{
export_results(
  x,
  dir = "multihostSIR_outputs",
  formats = c("csv", "xlsx"),
  prefix = "multihostSIR",
  overwrite = TRUE
)
}
\arguments{
\item{x}{A \code{"multihost_sir"} object from \code{\link[=run_multihost_sir]{run_multihost_sir()}}, or a
\code{"multihost_fixed"} object from \code{\link[=run_fixed_community]{run_fixed_community()}}.}

\item{dir}{Directory to write into. Created if it does not exist.}

\item{formats}{Which formats to write. Any of \code{"csv"} and \code{"xlsx"}.}

\item{prefix}{File-name prefix for the written files.}

\item{overwrite}{Overwrite an existing workbook of the same name.}
}
\value{
Invisibly, a character vector of the paths written.
}
\description{
Writes the tables from a run to CSV, and optionally to a single Excel
workbook. Accepts either a replicate study from \code{\link[=run_multihost_sir]{run_multihost_sir()}} or a
single deterministic run from \code{\link[=run_fixed_community]{run_fixed_community()}}; the set of tables
written matches the object.
}
\details{
Excel sheets are capped at 1,048,576 rows. When a table exceeds that
limit, the workbook records a pointer note instead of the full table; the
complete data are always written to CSV regardless.

Writing Excel requires the \pkg{openxlsx} package, which is a suggested
rather than a required dependency.
}
\examples{
res <- run_multihost_sir(3:4, n_iterations = 3, duration = 100, seed = 1)
out <- file.path(tempdir(), "multihost_demo")
export_results(res, dir = out, formats = "csv")
list.files(out)

}
