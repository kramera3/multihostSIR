% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_trajectories}
\alias{plot_trajectories}
\title{Mean epidemic trajectories}
\usage{
plot_trajectories(x, max_levels = 7L, xlim = NULL)
}
\arguments{
\item{x}{A \code{"multihost_sir"} object from \code{\link[=run_multihost_sir]{run_multihost_sir()}}.}

\item{max_levels}{Maximum number of richness levels to display. When more
levels were simulated, an evenly spaced subset is shown so the axis stays
readable. All data remain in \code{x$results}.}

\item{xlim}{Optional length-2 numeric vector limiting the time axis. Defaults
to the full horizon.}
}
\value{
A ggplot object.
}
\description{
Mean daily community-wide prevalence for each richness level.
}
\examples{
res <- run_multihost_sir(3:6, n_iterations = 5, duration = 150, seed = 1)
plot_trajectories(res, xlim = c(0, 120))
}
