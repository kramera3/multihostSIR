% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{check_dependencies}
\alias{check_dependencies}
\title{Check that dependencies are installed and current}
\usage{
check_dependencies(include_optional = TRUE, quiet = FALSE)
}
\arguments{
\item{include_optional}{Include optional packages in the report.}

\item{quiet}{Suppress the printed report and return the table silently.}
}
\value{
Invisibly, a data frame with one row per package and columns
\code{package}, \code{type}, \code{needed}, \code{installed}, \code{version} and \code{ok}.
}
\description{
Reports which of the packages needed by \strong{multihostSIR} are installed, which
are missing, and which are older than the minimum version the package
expects. Required packages are normally guaranteed by R at install time;
this function is most useful for diagnosing a partially broken library, or
for checking the optional packages that unlock extra features.
}
\examples{
check_dependencies()

# Just the machine-readable table
deps <- check_dependencies(quiet = TRUE)
deps[!deps$ok, ]

}
\seealso{
\code{\link[=install_dependencies]{install_dependencies()}}
}
