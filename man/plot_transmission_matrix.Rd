% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_transmission_matrix}
\alias{plot_transmission_matrix}
\title{Transmission matrix heat map}
\usage{
plot_transmission_matrix(
  community,
  beta = transmission_matrix(community),
  show_values = TRUE,
  relative = FALSE
)
}
\arguments{
\item{community}{A community data frame from \code{\link[=build_community]{build_community()}}.}

\item{beta}{Optional transmission matrix. Defaults to
\code{transmission_matrix(community)}.}

\item{show_values}{If \code{TRUE} (default), print the actual transmission rate
inside each tile. Turn off for large communities where the numbers crowd.}

\item{relative}{If \code{FALSE} (default), colour tiles by their actual value, so
the legend reads in transmission-rate units. Set \code{TRUE} to colour by value
relative to the matrix maximum instead, which can help when rates span
orders of magnitude.}
}
\value{
A ggplot object.
}
\description{
Visualises the asymmetric between-species transmission matrix. Rows are
recipients of infection, columns are sources, so the blocked
predator-to-prey corner is immediately visible under the default trophic
coefficients.
}
\examples{
set.seed(42)
comm <- build_community(6)
plot_transmission_matrix(comm)                    # actual values, labelled
plot_transmission_matrix(comm, show_values = FALSE)
}
