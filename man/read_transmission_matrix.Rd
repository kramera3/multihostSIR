% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_transmission_matrix}
\alias{read_transmission_matrix}
\title{Read a transmission matrix from a matrix, CSV, or Excel file}
\usage{
read_transmission_matrix(x, community = NULL, sheet = NULL)
}
\arguments{
\item{x}{A numeric matrix, or a path to a \code{.csv}, \code{.xlsx}, or \code{.xls} file
holding a square grid of numbers. A header row and first-column labels are
allowed and are used to check the ordering against the community.}

\item{community}{Optional community (from \code{\link[=read_community]{read_community()}} or
\code{\link[=build_community]{build_community()}}). When given, the matrix dimension is checked against
the number of species, and any row/column labels are checked against the
community's species names.}

\item{sheet}{For Excel input, the sheet name or number.}
}
\value{
A numeric \eqn{n \times n} matrix.
}
\description{
Reads an \eqn{n \times n} matrix of between-species transmission rates. Row
\eqn{i}, column \eqn{j} is the rate at which one infected individual of
species \eqn{j} infects susceptibles of species \eqn{i}.
}
\examples{
m <- matrix(c(0.05, 0.01, 0.00,
              0.01, 0.03, 0.02,
              0.00, 0.02, 0.04), nrow = 3, byrow = TRUE)
beta <- read_transmission_matrix(m)
beta

\dontrun{
beta <- read_transmission_matrix("my_matrix.xlsx")
}

}
\seealso{
\code{\link[=read_community]{read_community()}}, \code{\link[=run_fixed_community]{run_fixed_community()}}, \code{\link[=transmission_matrix]{transmission_matrix()}}
}
